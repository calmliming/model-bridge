# Sub2API 分组问题 - 手动检查清单

## ✅ 你需要做的检查（在浏览器中）

### 1. 打开管理后台
访问：http://localhost:3003

### 2. 登录
使用你的管理员账号登录

---

### 第一步：检查分组成员列表 ⭐ 最重要

1. 点击左侧菜单：**分组管理** (Account Groups)
2. 找到你创建的分组，点击查看详情
3. 查看 **成员列表** 或 **Members** 区域

**请回答：**
- [ ] 成员列表中是否显示了这三个 sub2api 账户？
- [ ] 如果没有 → **这就是问题所在！**

**解决方法：**
- 点击 **编辑成员** 或 **Add Members** 按钮
- 勾选三个 sub2api 账户
- 点击 **保存**
- 刷新页面验证

---

### 第二步：检查账户状态

1. 点击左侧菜单：**账户管理** (Accounts)
2. 找到三个 sub2api 账户

**请检查每个账户：**
- [ ] 状态 (Status) 是否为 `active`（绿色）
  - 如果是 `error` 或 `rate_limited`（红色）→ 账户在冷却中
- [ ] 是否有冷却倒计时显示
- [ ] Proxy URL 字段是否已填写
- [ ] 权重 (Weight) 是否合理（通常是 1）

**如果账户在冷却中：**
- 点击账户操作菜单（三个点或齿轮图标）
- 选择 **清除冷却** 或 **Reset Status**
- 或者等待 15 秒后自动恢复

---

### 第三步：检查 API Key 绑定

1. 点击左侧菜单：**API Keys**
2. 找到你正在使用的 API Key

**请检查：**
- [ ] API Key 是否启用 (Enabled)
- [ ] **账户分组** 或 **Account Group** 字段是否选择了正确的分组
  - 如果显示"默认池"或为空 → 需要绑定到分组

**解决方法：**
- 点击编辑 API Key
- 在"账户分组"下拉框中选择目标分组
- 保存

---

### 第四步：查看错误日志

1. 点击左侧菜单：**使用记录** (Usage Logs)
2. 筛选 Status = `error` 或查看最近的失败记录

**请记录：**
- 失败的时间
- 使用的账户 ID
- 错误状态
- 模型名称

---

## 📋 诊断结果表

请填写以下信息：

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 分组成员列表是否包含3个账户 | ☐ 是 ☐ 否 | |
| 账户1状态 | ☐ active ☐ error ☐ rate_limited | |
| 账户2状态 | ☐ active ☐ error ☐ rate_limited | |
| 账户3状态 | ☐ active ☐ error ☐ rate_limited | |
| 账户1是否有冷却倒计时 | ☐ 是 ☐ 否 | |
| 账户2是否有冷却倒计时 | ☐ 是 ☐ 否 | |
| 账户3是否有冷却倒计时 | ☐ 是 ☐ 否 | |
| 账户1的proxy_url | 已配置 / 未配置 | |
| 账户2的proxy_url | 已配置 / 未配置 | |
| 账户3的proxy_url | 已配置 / 未配置 | |
| API Key绑定的分组 | 分组名 / 默认池 | |
| 最近错误记录数量 | 数量: ___ | |

---

## 🎯 根据结果判断问题

### 情况 A：分组成员列表为空或不完整
**问题：** 成员关系未建立  
**解决：** 在分组管理页面重新添加账户

### 情况 B：所有账户都在冷却中
**问题：** 配置错误或上游故障导致连续失败  
**解决：** 清除冷却状态 + 检查 proxy_url 配置

### 情况 C：API Key 未绑定分组
**问题：** API Key 使用默认池而不是分组  
**解决：** 编辑 API Key，选择正确的分组

### 情况 D：proxy_url 未配置
**问题：** sub2api 账户缺少转发地址  
**解决：** 为每个账户配置正确的 proxy_url

---

## 💡 快速测试

修复后，可以通过以下方式快速测试：

```bash
curl -X POST http://localhost:3003/v1/messages \
  -H "Authorization: Bearer your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-3-5-sonnet-20241022",
    "max_tokens": 100,
    "messages": [{"role": "user", "content": "test"}]
  }'
```

或者在管理后台的使用记录中查看最新的请求是否成功。

---

## 📸 如果可以的话，提供截图会很有帮助

特别是：
1. 分组详情页的成员列表
2. 三个 sub2api 账户的状态
3. 最近的错误日志

这样我可以更准确地判断问题！
