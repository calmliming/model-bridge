@AGENTS.md

## Claude Code

- 本文件遵循 Claude Code 的项目记忆约定。Claude 会在会话开始时读取 `CLAUDE.md`。
- 共享项目规则放在 `AGENTS.md`；通用规则优先写在那里，确保 Codex 和 Claude 使用同一套约定。
- 只有不适合 Codex 的 Claude 专属流程说明，才写在本文件里。
- 在 Claude Code 中可用 `/memory` 确认本文件和导入的规则是否已加载。
- 用中文总结。
- 每次提交（commit message）必须用中文书写，并按序号（1. 2. 3. …）详细说明本次改动的内容与原因。
