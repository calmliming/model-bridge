# Sub2API 分组问题 - 完整诊断包

## 📁 文件清单

1. **remote-diagnosis-guide.md** ⭐ - 无需数据库的远程诊断指南（推荐先看这个）
2. **diagnose-sub2api-group.ts** - 自动诊断脚本（需要数据库）
3. **README-DIAGNOSIS.md** - 详细的诊断和解决方案手册
4. **sub2api-group-issue-analysis.md** - 技术原理分析

## 🎯 问题描述

将三个 sub2api 账户放在同一个分组下后，账户无法正常使用。

## 🔍 最可能的原因（按概率排序）

### 1. 账户未正确加入分组（概率：80%）
**症状：** 账户看起来在分组里，但实际请求时找不到可用账户

**原因：** 
- 在管理界面设置了分组，但 `account_group_members` 表中没有对应记录
- 账户调度器使用 `INNER JOIN` 查询成员关系，没有记录就查不到

**检查方法：**
- 打开管理后台 → 进入分组详情页
- 查看"成员列表"是否显示这三个账户
- 如果列表为空或不完整 → 就是这个问题

**解决方法：**
- 在管理后台重新将账户添加到分组
- 或通过 SQL 手动插入成员关系（见 README-DIAGNOSIS.md）

### 2. 所有账户都在冷却中（概率：15%）
**症状：** 请求失败，提示无可用账户

**原因：**
- 配置错误（如 proxy_url 错误）导致三个账户连续失败
- sub2api 账户失败后会冷却 15 秒
- 如果在 15 秒内三个都失败了，就会全部不可用

**检查方法：**
- 打开管理后台 → 账户列表
- 查看状态列是否显示 "rate_limited" 或 "error"
- 查看是否有冷却倒计时

**解决方法：**
- 检查并修正 proxy_url 配置
- 手动清除冷却状态
- 等待冷却时间过期（15秒后自动恢复）

### 3. API Key 未绑定到分组（概率：5%）
**症状：** 账户配置正确，但仍然无法使用

**原因：**
- API Key 的 `account_group_id` 字段为空
- 系统会从"默认池"（未分组的账户）中选择
- 但这三个账户已经在分组中，不在默认池里

**检查方法：**
- 打开管理后台 → API Keys 页面
- 查看你使用的 API Key 详情
- 检查"绑定分组"字段是否为空

**解决方法：**
- 编辑 API Key，选择正确的分组

## 🚀 快速诊断步骤（无需数据库）

### 步骤 1：访问管理后台
```
http://localhost:3001  （或你的部署地址）
```

### 步骤 2：检查账户状态
进入 **账户管理** 页面，确认：
- [ ] 三个 sub2api 账户都存在
- [ ] 状态都是 "active"（不是 error 或 rate_limited）
- [ ] 没有冷却倒计时
- [ ] proxy_url 字段已正确填写

### 步骤 3：检查分组成员
进入 **分组管理** → 点击目标分组，确认：
- [ ] 成员列表中显示这三个账户
- [ ] 每个账户的权重设置正常

⚠️ **如果成员列表为空或不完整 → 这就是问题所在！**

### 步骤 4：检查 API Key
进入 **API Keys** 页面，确认：
- [ ] 你使用的 API Key 已启用
- [ ] 绑定到了正确的分组（不是"默认池"）

### 步骤 5：查看错误日志
进入 **使用记录** 页面：
- 查看最近的失败记录
- 记录失败的时间、账户、错误状态

## 🔧 常见解决方案

### 解决方案 A：重新添加账户到分组
```
1. 管理后台 → 分组管理 → 选择目标分组
2. 点击"添加成员"或"编辑成员"
3. 选择三个 sub2api 账户
4. 保存
5. 重新测试
```

### 解决方案 B：清除账户冷却状态
```
1. 管理后台 → 账户管理
2. 找到状态为 error 或 rate_limited 的账户
3. 点击"清除冷却"或"重置状态"按钮
4. 或者等待 15 秒后自动恢复
```

### 解决方案 C：修正 proxy_url
```
1. 管理后台 → 账户管理
2. 编辑每个 sub2api 账户
3. 确认 proxy_url 格式正确：https://your-sub2api-server.com
4. 保存并测试
```

### 解决方案 D：绑定 API Key 到分组
```
1. 管理后台 → API Keys
2. 编辑你使用的 API Key
3. 在"账户分组"下拉框中选择目标分组
4. 保存
```

## 📊 技术原理（可选阅读）

### 为什么分组账户需要成员关系记录？

代码中的账户选择逻辑（`src/accounts/scheduler.ts:55-75`）：

```typescript
const rows = groupId
  ? await db
      .select({...})
      .from(accounts)
      .innerJoin(accountGroupMembers, eq(accountGroupMembers.accountId, accounts.id))
      .where(and(
        eq(accounts.provider, provider),
        ne(accounts.status, 'disabled'),
        eq(accountGroupMembers.groupId, groupId),
      ))
  : // 未分组账户的查询
```

**关键点：**
1. 使用了 `INNER JOIN` - 必须同时在两个表中存在
2. 查询条件包括 `eq(accountGroupMembers.groupId, groupId)` - 必须匹配分组ID
3. 如果 `account_group_members` 表中没有记录，查询结果为空

### 为什么会同时冷却？

Sub2API 的特殊处理（`src/routes/relay.ts:99`）：

```typescript
const RELAY_TO_RELAY_COOLDOWN_MS = 15_000  // 15秒
```

**失败流程：**
1. 请求使用账户 A → 失败 → 冷却 15 秒
2. 重试使用账户 B → 失败 → 冷却 15 秒  
3. 重试使用账户 C → 失败 → 冷却 15 秒
4. 最大重试次数（3次）用完 → 请求失败
5. 接下来 15 秒内所有请求都会失败

## 📞 获取帮助

如果按照上述步骤仍无法解决，请提供：

1. **管理后台截图**（必需）：
   - 三个 sub2api 账户的状态
   - 分组详情页的成员列表
   - 使用的 API Key 配置

2. **错误信息**（必需）：
   - 使用记录中最近 3 条失败的详情

3. **配置信息**（可选）：
   - proxy_url 的值
   - 分组的费率乘数设置

## 📝 总结

**最有可能的问题：** 账户虽然看起来在分组中，但 `account_group_members` 表缺少记录

**最快的验证方法：** 打开管理后台 → 分组详情 → 查看成员列表是否完整

**最简单的解决方法：** 重新在界面上添加账户到分组

---

💡 **提示：** 先按照 `remote-diagnosis-guide.md` 的步骤检查，大多数问题都能通过管理后台界面解决，无需直接操作数据库。
