# Sub2API 分组问题 - 修复报告

## 📋 问题描述

将三个 sub2api 账户放在同一个分组下后，账户无法正常使用。

## 🔍 诊断发现

### 原始问题
1. **账户权重不平衡**
   - `MagicAI`: 权重 1
   - `sub2api-claude`: 权重 2
   - `sub2api-openai`: 权重 2

2. **账户选择逻辑**
   - 系统优先选择权重高的账户
   - `sub2api-openai` (权重2) 被优先选择
   - 该账户连续失败，导致无法使用其他账户

3. **错误日志分析**
   - 所有 3 条错误都来自 `sub2api-openai` 账户
   - 使用的 proxy_url: `https://api.ymocode.com`
   - 错误发生在 11:05-11:07 之间
   - 模型：`claude-opus-4-8`, `deepseek-v4-pro`

### 配置状态
✅ 三个账户都已正确加入分组"明明自用分组"  
✅ 账户状态都是 active  
✅ API Key "明明自用"已绑定到正确的分组  
❌ 权重不平衡导致无法轮换  

## ✅ 修复操作

### 执行时间
2026-07-22 13:00 (约)

### 修复步骤

1. **平衡账户权重**
   ```sql
   UPDATE accounts
   SET weight = 1
   WHERE provider = 'sub2api'
   ```
   - 将所有三个账户的权重统一设置为 1
   - 确保负载均匀分配

2. **清除冷却状态**
   ```sql
   UPDATE accounts
   SET status = 'active', cooldown_until = NULL
   WHERE provider = 'sub2api'
     AND status IN ('error', 'rate_limited')
   ```
   - 所有账户都已是 active 状态（无需清除）

3. **验证配置**
   - ✅ 所有账户权重已设置为 1
   - ✅ 所有账户状态为 active
   - ✅ 所有账户都在分组中
   - ✅ 无冷却时间

## 📊 修复后状态

| 账户 | 权重 | 状态 | 分组 | Proxy URL |
|------|------|------|------|-----------|
| MagicAI | 1 | active | 明明自用分组 | https://sky1818.com |
| sub2api-claude | 1 | active | 明明自用分组 | https://api.ymocode.com |
| sub2api-openai | 1 | active | 明明自用分组 | https://api.ymocode.com |

## 🎯 预期效果

修复后，系统会按照 **加权 LRU (Least Recently Used)** 算法选择账户：

1. **权重相同**（都是 1）→ 按最后使用时间排序
2. **选择最久未使用的账户** → 自动轮换
3. **负载均匀分配** → 三个账户都会被使用

### 轮换示例
```
请求 1 → MagicAI (最久未使用)
请求 2 → sub2api-claude (次久未使用)
请求 3 → sub2api-openai (次久未使用)
请求 4 → MagicAI (又变成最久未使用)
...循环
```

## 🧪 验证方法

### 方法 1：通过管理后台观察

1. 访问 `http://localhost:3003`
2. 进入 **使用记录** 页面
3. 发送几次测试请求
4. 观察 **账户** 列是否在三个账户间轮换

### 方法 2：通过 API 测试

```bash
# 使用你的 API Key 发送测试请求
curl -X POST http://localhost:3003/v1/messages \
  -H "Authorization: Bearer mb-b8e351d6..." \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-3-5-sonnet-20241022",
    "max_tokens": 100,
    "messages": [{"role": "user", "content": "test 1"}]
  }'

# 重复几次，每次改变内容
```

### 方法 3：查询数据库

```bash
npx tsx diagnose-sub2api-group.ts
```

查看使用日志中是否有不同账户的记录。

## ⚠️ 注意事项

### 如果仍有错误

如果修复后仍然出现错误，可能是 **proxy_url 端点配置问题**：

1. **检查 `https://api.ymocode.com` 端点**
   - 是否需要额外的认证信息
   - 是否支持你请求的模型
   - 是否有余额或配额限制

2. **检查 `https://sky1818.com` 端点**
   - 同上

3. **临时禁用有问题的账户**
   - 在管理后台将状态改为 disabled
   - 测试其他账户是否正常

### 监控建议

定期检查：
1. **使用记录** - 确认账户轮换正常
2. **账户状态** - 确认无长时间冷却
3. **错误日志** - 识别特定账户或端点的问题

## 📁 相关文件

- `fix-sub2api.ts` - 修复脚本（已执行）
- `diagnose-sub2api-group.ts` - 诊断脚本
- `SOLUTION.md` - 完整解决方案文档
- `手动检查清单.md` - 浏览器手动检查步骤

## ✅ 修复总结

**问题根源：** 权重不平衡导致系统一直优先选择同一个账户（sub2api-openai），该账户失败后无法自动切换到其他账户。

**解决方案：** 将所有账户权重设置为 1，启用 LRU 轮换机制。

**修复状态：** ✅ 完成

**验证状态：** ⏳ 待测试（请发送实际请求验证）

---

**修复时间：** 2026-07-22  
**修复工具：** fix-sub2api.ts  
**执行者：** Claude (Opus 4.8)
