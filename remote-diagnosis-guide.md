# 远程诊断 Sub2API 分组问题

## 情况说明
本地环境无法启动数据库，需要通过远程服务器或其他方式进行诊断。

## 选项 1：通过 Web 管理后台检查（推荐）

### 1. 访问管理后台
打开浏览器访问：`http://your-server:3001`（或实际部署的地址）

### 2. 检查账户状态
进入 **账户管理** 页面：

**需要确认的信息：**
- [ ] 三个 sub2api 账户是否都显示为 "active" 状态
- [ ] 是否有账户显示冷却倒计时
- [ ] proxy_url 字段是否正确填写
- [ ] 账户是否显示在同一个分组下

**如果账户状态为 "error" 或 "rate_limited"：**
→ 这表示账户最近失败了，需要清除冷却状态或检查配置

**如果看不到分组信息：**
→ 可能是账户虽然设置了分组，但成员关系没有建立

### 3. 检查分组配置
进入 **分组管理** 页面：

**需要确认的信息：**
- [ ] 目标分组是否存在
- [ ] 点击分组，查看成员列表是否包含这三个 sub2api 账户
- [ ] 每个账户的权重设置是否合理

**如果分组成员列表为空或不完整：**
→ 这就是问题所在！需要重新添加账户到分组

### 4. 检查 API Key 配置
进入 **API Keys** 页面：

**需要确认的信息：**
- [ ] 你使用的 API Key 是否绑定到了该分组
- [ ] API Key 状态是否为 "enabled"

**如果 API Key 没有绑定分组：**
→ 编辑 API Key，选择正确的分组

### 5. 查看使用日志
进入 **使用记录** 页面：

**查看最近的失败记录：**
- 失败的时间
- 使用的账户
- 错误状态（status 字段）
- 是否所有失败都集中在这三个账户

## 选项 2：通过 API 接口诊断

如果可以访问服务器，可以使用 curl 或浏览器直接调用 API：

### 获取账户列表
```bash
# 先登录获取 token（替换实际的用户名密码）
curl -X POST http://your-server:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin","password":"admin"}'

# 复制返回的 token，然后查询账户
curl http://your-server:3001/api/admin/accounts \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 查看响应内容
查找 provider 为 "sub2api" 的账户，检查：
- `status` 字段（应该是 "active"）
- `cooldownUntil` 字段（应该是 null 或已过期的时间戳）
- `proxyUrl` 字段（应该有正确的 URL）

## 选项 3：在服务器上运行诊断脚本

如果你可以 SSH 到部署服务器：

```bash
# 进入项目目录
cd /path/to/model-bridge

# 运行诊断脚本
docker compose exec model-bridge npx tsx /app/diagnose-sub2api-group.ts

# 或者如果数据库是独立运行的
npx tsx diagnose-sub2api-group.ts
```

## 选项 4：请他人帮忙

将以下文件发送给能访问数据库的人：
1. `diagnose-sub2api-group.ts` - 诊断脚本
2. `README-DIAGNOSIS.md` - 操作说明

他们只需要：
```bash
npx tsx diagnose-sub2api-group.ts
```

然后将输出结果发给你即可。

## 常见问题快速自查清单

### ✅ 问题自查表

请在管理后台或通过 API 确认以下信息：

| 检查项 | 预期状态 | 实际状态 | 问题？ |
|--------|---------|---------|--------|
| 三个 sub2api 账户都存在 | ✅ 是 | ? | |
| 账户状态都是 "active" | ✅ 是 | ? | |
| 账户没有冷却倒计时 | ✅ 否 | ? | |
| 账户的 proxy_url 已配置 | ✅ 是 | ? | |
| 分组已创建 | ✅ 是 | ? | |
| 三个账户都在分组成员列表中 | ✅ 是 | ? | |
| API Key 绑定到了该分组 | ✅ 是 | ? | |
| 最近的错误日志数量 | ⚠️ 少 | ? | |

### 🔧 根据结果判断问题

**情况 A：账户不在分组成员列表中**
```
问题：分组成员关系未建立
解决：在管理后台重新将账户添加到分组
```

**情况 B：所有账户都在冷却中**
```
问题：配置错误导致连续失败
解决：
1. 检查 proxy_url 是否正确
2. 检查上游 sub2api 服务是否正常
3. 在管理后台手动清除冷却状态
```

**情况 C：API Key 未绑定分组**
```
问题：API Key 仍在使用默认池
解决：编辑 API Key，选择正确的分组
```

**情况 D：proxy_url 未配置**
```
问题：sub2api 账户缺少转发地址
解决：为每个账户配置正确的 proxy_url
```

## 临时解决方案

如果急需使用，可以尝试：

1. **将账户移出分组** - 先单独使用一个账户测试
2. **清除冷却状态** - 在管理后台操作或通过 API
3. **创建新的测试 API Key** - 绑定到该分组，看是否能正常工作

## 需要提供的信息

如果问题仍未解决，请提供以下信息：

1. **管理后台截图：**
   - 三个 sub2api 账户的列表页
   - 分组详情页（显示成员列表）
   - 使用的 API Key 配置页

2. **错误信息：**
   - 最近3-5条失败的使用记录
   - 包括时间、状态、使用的账户

3. **配置信息：**
   - 这三个账户的 proxy_url 是否相同
   - 分组是否设置了特殊的费率乘数

有了这些信息，可以更准确地定位问题！
