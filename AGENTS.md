# AGENTS.md

## Codex

- Codex 在每次会话开始时必须读取仓库根目录的 `AGENTS.md` 和 `CLAUDE.md`，并遵守其中所有适用于 Codex 的项目规则。
- 生成 Markdown 文档时，文件必须放在 `docs/` 目录下，并使用“中文名称_时间”的命名格式；时间统一使用 `YYYYMMDD-HHmmss`，例如 `接口变更说明_20260809-191500.md`。
